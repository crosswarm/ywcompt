# 方案评审

- 反对按“权限申请单”名称隐藏按钮：同名单据可能分别属于 MDF 与 YPD，改为框架能力判断。
- 反对仅在前端隐藏：保留审批执行器的服务端拒绝，防止绕过页面调用。
- 反对把 MDF 文档的 `batchagree` 直接视为后端协议：继续使用现有 CLI 动作刷新与批量审批契约。
- 反对为 YPD 猜测审批接口：现阶段只恢复详情，审批能力保持关闭。

用户已在 2026-07-15 确认该方案并要求开始实施。
