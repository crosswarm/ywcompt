# Todo

Status: implemented

- [x] 扩展 action snapshot 字段。
- [x] 补 MDF/iForm/YNF 轻量 metadata 抽取。
- [x] 让 rich detail 写入 raw/meta/normalized/observedActions。
- [x] handler 增加 `refreshActions()` 与 `approvalStrategy()`。
- [x] executor 执行前刷新动作并按策略分发。
- [x] 更新 TypeScript 类型、JSON schema、SKILL 和 spec。
- [x] 增加覆盖测试。
